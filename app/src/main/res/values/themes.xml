<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Net" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorOnPrimary">@color/primaria</item>
        <item name="colorOnPrimaryContainer">@color/primaria_variacao</item>
        <item name="colorSecondary">@color/secundaria</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="Theme.Net" parent="Base.Theme.Net" />
</resources>